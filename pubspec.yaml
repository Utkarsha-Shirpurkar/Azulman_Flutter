name: azulman
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  flutter_otp_text_field: ^1.0.0
  http: ^0.13.4
  form_field_validator: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  sms_autofill: ^2.2.0

flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  assets:
    - images/test_logo.png
    - images/azulman_bg.png
